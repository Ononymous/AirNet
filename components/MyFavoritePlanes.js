import { useState } from 'react';

const useFavoritePlanes = () => {
  // const [favoritePlanes, setFavoritePlanes] = useState([]);

  // const addFavoritePlane = (plane) => {
  //   setFavoritePlanes([...favoritePlanes, plane]);
  // };

  // const removeFavoritePlane = (plane) => {
  //   setFavoritePlanes(favoritePlanes.filter((p) => p !== plane));
  // };

  // return [favoritePlanes, addFavoritePlane, removeFavoritePlane];
};


export default useFavoritePlanes;